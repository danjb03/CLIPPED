fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
pydantic==2.10.4

# Phase 1 — ingest + transcribe (local, free)
yt-dlp==2025.1.15
faster-whisper==1.1.0
# Diarisation (speaker labels). pyannote pulls in torch/torchaudio.
pyannote.audio==3.3.2

# Phase 2/6 — clip selection + copy
anthropic==0.42.0

# Phase 4 — split-screen face detection
# mediapipe==0.10.18
# opencv-python==4.10.0.84
